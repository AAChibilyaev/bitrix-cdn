apiVersion: 1

providers:
  - name: 'Bitrix CDN Dashboards'
    orgId: 1
    folder: 'CDN'
    folderUid: 'cdn'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards